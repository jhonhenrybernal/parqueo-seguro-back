import './bootstrap';
import { createApp } from 'vue';
import ParkingApp from './components/ParkingApp.vue';

// Crea y monta la aplicación de Vue en el elemento con id "app"
createApp(ParkingApp).mount('#app');
